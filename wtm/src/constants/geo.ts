/** Single source of truth for geo fallbacks — was previously duplicated in 3 files. */
export const PITTSBURGH = { lat: 40.4406, lng: -79.9959 } as const;

export const DEFAULT_MAP_DELTA = { latitudeDelta: 0.06, longitudeDelta: 0.06 } as const;
